import _ from "lodash";

class ConferenceService {
    getConferences() {
        return null;
    }

    static getClientMessage(conference) {
        if (conference["totalClients"] !== 0)
            return
    }
}

export default ConferenceService;
